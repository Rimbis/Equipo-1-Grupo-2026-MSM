import {useEffect,useState} from 'react'; import {obtenerCategorias} from '../services/api';
export default function Categorias(){const [datos,setDatos]=useState([]),[error,setError]=useState('');useEffect(()=>{obtenerCategorias().then(setDatos).catch(e=>setError(e.message))},[]);return <section><div className="encabezado-pagina"><h1>Categorías</h1><p>Categorías disponibles para organizar los objetos.</p></div>{error&&<p className="mensaje error">{error}</p>}<div className="lista-categorias">{datos.map(c=><div className="categoria" key={c.id}><strong>{c.nombre}</strong></div>)}</div>{!datos.length&&<p className="vacio">No hay categorías para mostrar.</p>}</section>}
